server:
  port: 8002

spring:
  application:
    name: post-service
  redis:
    host: ${SPRING_REDIS_HOST:127.0.0.1}
    port: ${SPRING_REDIS_PORT:6379}
    database: 0 # Redis数据库索引（默认为0）
    timeout: 180000 # 连接超时时间（毫秒）
    password: ${REDIS_PASSWORD:1234546}
  cloud:
    loadbalancer:
      enabled: true
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:http://127.0.0.1:8848/}
  datasource:
    url: jdbc:mysql://${DATABASE_HOST:127.0.0.1}:${DATABASE_PORT:3306}/${DATABASE_NAME:wevote}?characterEncoding=UTF-8
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:123456}
    ## driver-class-name: com.mysql.cj.jdbc.Driver # 注意：建议使用MySQL 8+的驱动
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com/fizzy/core/entity


#MyBatis使用pageHelper分页
pagehelper:
  helper-dialect: mysql
  support-methods-arguments: true